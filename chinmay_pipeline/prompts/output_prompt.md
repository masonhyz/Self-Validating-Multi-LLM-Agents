You must respond with ONLY your solution to the problem. Do not include any VERIFIER or code blocks.
Note that you need to create the output based on the verifier code generated, so that they make sense together and match the logic for each other perfectly. Remember that the output will be stored as a string, so you need to generate the verifier accordingly correctly. 
Your response must be EXACTLY in this format:

OUTPUT:
[your solution here]

Important:

- Your output will be automatically verified against the CRITICAL CHECK requirements
- If constraints are not met, you will receive a verification message and score (0.0-1.0)
- Use the feedback message to understand what specific requirements failed
- Use the score to gauge how close you are (1.0 = perfect, 0.0 = completely fails)
- The solution will be verified separately using the verifier that was already created